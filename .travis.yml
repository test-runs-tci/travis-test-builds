language: shell
os: linux
dist: focal
install: skip
script:
- sleep 10
deploy:
  provider: script
  script: ruby --version
  dpl_version: "1.10.16"
  on:
    all_branches: true
jobs:
  include:
    - dist: xenial
    - dist: bionic
    - dist: focal
    - dist: jammy
    - dist: noble
