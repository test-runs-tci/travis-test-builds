language: php
php: '8.3'
dist: focal

install: skip
script:
  - |
    wrap_command_logs() {
      echo "Sleeping..." && echo -en "travis_fold:start:$1\\r"
      "$@"
      echo -en "travis_fold:end:$1\\r"
    }
    wrap_command_logs sleep sleep 30
