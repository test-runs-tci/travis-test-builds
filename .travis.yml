language: shell
dist: focal
cache:
  directories:
    - $HOME/.gradle/caches/
jobs:
  include:
    - script: sleep 1
      jdk: openjdk17
    - script: sleep 2
      jdk: openjdk21
    - script: sleep 3
      jdk: openjdk24
