language: shell
os: linux
dist: jammy

before_install:
  - curl -o- https://raw.githubusercontent.com/ArdaGnsrn/pvm-ubuntu/master/install.sh | sudo bash
  - pvm list
  - pvm install 8.3
  - pvm use 8.3

script: php -v
