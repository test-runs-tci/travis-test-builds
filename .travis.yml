language: erlang
otp_release:
  - 20.3.8
os:
- linux
before_install:
- sleep 2
install: skip
script:
- erl -eval 'erlang:display(erlang:system_info(otp_release)), halt().'  -noshell
jobs:
  include:
    - dist: bionic
    - dist: focal
    - dist: jammy
    - dist: noble
